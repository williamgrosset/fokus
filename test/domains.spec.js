var assert = require('assert');
describe('domains', function() {
    /*
    describe('addDomain()', function() {
        it('should return valid', function() {
            assert.equal(1, [1,2,3].indexOf(1));
        });
    });
    describe('addDomain()', function() {
        it('should not return valid', function() {
            assert.equal(1, [1,2,3].indexOf(1));
        });
    });
    */
});
